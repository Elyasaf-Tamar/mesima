# תיקון הסנכרון — משימה 4.7.1

## מה הייתה התקלה

גיבוי ידני בטלפון השתמש בחיבור Android של Firebase, ואילו הסנכרון האוטומטי הוריד קבצים מתוך הדף הפנימי של האפליקציה. אלה היו שני מסלולי חיבור שונים.

התקלה שוחזרה בבדיקה חיה של החשבון הקיים במחשב: קריאת מצב הסנכרון מ־Firestore הצליחה, אבל הורדת קובץ הנתונים ב־Chrome החזירה בדיוק **TypeError: Failed to fetch**. הורדת אותו קובץ מחוץ למסלול הדפדפן הצליחה. זה תואם חסימת גישה בין מקורות בדפדפן — CORS — ולא היעדר קובץ או חשבון מנותק.

ב־4.7.1 הסנכרון משתמש בחיבור של מעטפת Android או Windows. ההרשאות נשארות מוגבלות לחשבון המחובר ולקובצי הסנכרון שלו; לא שונו כללי Firebase ולא נפתחה גישה ציבורית. נוסף גם פירוט של שלב הכשל, והבחנה בין ניתוק, פקיעת זמן ושגיאת הרשאה.

## מה להתקין

1. בטלפון, התקן את **Mesima-4.7.1-Android-1.6-Firebase.apk** מעל האפליקציה הקיימת. אין להסיר אותה ואין צורך לשחזר גיבוי.
2. במחשב, צא ממשימה דרך תפריט **משימה → יציאה**, כולל הסמל ליד השעון. חלץ את **Mesima-4.7.1-Windows.zip** ופתח את Mesima.exe שבתיקייה החדשה, או הפעל את המתקין המצורף.
3. פתח את האפליקציה בשני המכשירים. בטלפון צריכות להופיע גרסת דף **4.7.1** ומעטפת **1.6**. במחשב צריכה להופיע **4.7.1**.
4. בהגדרות → גיבוי ונתונים → גיבוי בענן, ודא ששני המכשירים מחוברים לאותו חשבון ושהסנכרון האוטומטי מופעל.
5. השאר את שתי האפליקציות פתוחות ואת חלונות העריכה סגורים, והמתן עד כדקה. אמור להופיע **מסונכרן**.
6. צור משימת ניסיון בטלפון ובדוק שהיא מופיעה במחשב. שנה את התיאור שלה במחשב ובדוק שהשינוי מגיע לטלפון.

החיבור הקיים אמור להישמר. אין צורך להחליף Rules, ליצור חשבון חדש, למחוק נתונים או לכבות ולהפעיל את הסנכרון כדי לאפס אותו. עדכון HTML בלבד אינו מתקין את תיקון Android — צריך את ה־APK החדש.

## מה אומת

- בקובץ הסנכרון הקיים בענן, מסלול ההורדה החדש החזיר HTTP 200 וקובץ בגודל 188,660 בתים. הבדיקה הייתה בקריאה בלבד; נתוני החשבון לא שונו בענן.
- בדיקת שני מכשירים מדומים עברה גם כאשר כל בקשות fetch מתוך הדפדפן הוכשלו בכוונה. נבדקו העלאה, הורדה, מיזוג עריכות, מחיקות, התנגשות וניסיון חוזר לאחר ניתוק.
- עברו 21 בדיקות נתונים ותעבורה ו־10 בדיקות Android. גרסת Windows הארוזה נפתחה ונבדקה, והחתימה של ה־APK תואמת לגרסה הקיימת.

לא היה טלפון פיזי מחובר למחשב, ולכן עדיין צריך לבצע את בדיקת שני המכשירים לאחר ההתקנה. הסנכרון פועל כשהאפליקציה פתוחה; התיקון הזה אינו מוסיף סנכרון בזמן שהאפליקציה סגורה לחלוטין.

רקע טכני על גישה מדפדפן לקובצי Storage: [תיעוד Firebase — CORS](https://firebase.google.com/docs/storage/web/download-files#cors_configuration).
