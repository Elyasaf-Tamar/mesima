# פרסום גרסה של משימה

המאגר הוא https://github.com/Elyasaf-Tamar/mesima והענף הראשי הוא `main`.

1. עורכים את המקור ב־`src` ואת מעטפת Android ב־`android`.
2. מריצים `node tools/build.mjs` ואת בדיקות הגרסה. שומרים גם את `index.html` הבנוי בשורש ואת העותק שבנכסי Android.
3. מפרסמים את השינויים ל־`main`. GitHub Pages מגיש את האתר מאותה כתובת קיימת: https://elyasaf-tamar.github.io/mesima/index.html.
4. כשמשתנה קוד Android בונים מקומית APK חתום במפתח של האפליקציה המותקנת. אין להעלות את המפתח או את סיסמאותיו למאגר.
5. יוצרים Release עם תג הגרסה, לדוגמה `v4.8`, ומצרפים את ה־APK החתום, חבילת המקור וסכומי SHA-256. ב־4.8 קובץ ההתקנה הוא `Mesima-4.8-Android-1.7-Firebase.apk`. אין קובץ התקנה ל־Windows בפרסום הזה.
6. לאחר הפרסום בודקים את קובצי ההורדה ואת האתר החי. החלפת HTML אינה מתקינה שינויי Android; המשתמש מתקין את ה־APK כעדכון בלי להסיר את האפליקציה.

GitHub Actions מריץ את בדיקות JavaScript ו־Android ובונה APK לפיתוח בלבד. הוא אינו מפרסם APK חדש אוטומטית, כדי לא להחליף את ההתקנה החתומה בחתימת debug אחרת.

לסנכרון היסטוריית ביצוע וסיכומי יום ושבוע נדרשים לקוחות 4.8 בכל המכשירים המשתתפים בסנכרון. כללי Firebase נשארים כפי שהיו.
