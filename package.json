{
  "name": "mesima",
  "version": "4.8.1",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
